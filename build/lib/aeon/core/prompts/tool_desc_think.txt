Internal reasoning loop. Use sparingly and only when necessary. Use only when the query you are asking is something that is non-obvious, and requires complex, multi-step reasoning. 
Schema:
  query (str, required): The question or topic to reason about.
Example: {"tool_name": "think", "parameters": {"query": "What are the possible root causes of this import error?"}}

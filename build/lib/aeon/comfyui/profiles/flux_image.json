{
  "model_id": "flux_image",
  "display_name": "Flux.2-Dev FP8 piFlow (High Quality)",
  "description": "Flux.2-Dev FP8 txt2img via piFlow (steps=20, guidance=4.0, shift=3.5). Uses GPU1 (~40GB VRAM peak).",
  "output_type": "image",
  "workflow_file": "flux_image_api.json",
  "parameters": {
    "prompt": {"type": "string", "required": true},
    "width": {"type": "int", "required": false, "default": 1024},
    "height": {"type": "int", "required": false, "default": 1024},
    "steps": {"type": "int", "required": false, "default": 20},
    "guidance": {"type": "float", "required": false, "default": 4.0},
    "shift": {"type": "float", "required": false, "default": 3.5},
    "seed": {"type": "int", "required": false, "default": -1},
    "filename_prefix": {"type": "string", "required": false, "default": "flux_dev2"}
  }
}
Legacy compatibility capability, hidden from the protocol-v2 model tool set.
Use an `ask_user` turn with one focused question. The harness yields immediately
and resumes only when the next exact role=user message arrives.

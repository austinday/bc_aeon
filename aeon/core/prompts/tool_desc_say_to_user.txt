Legacy compatibility capability, hidden from the protocol-v2 model tool set.
Visible responses use the turn envelope directly: `final` for a terminal response
or `ask_user` for one question followed by a yield.

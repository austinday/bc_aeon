Legacy compatibility capability, hidden from the protocol-v2 model tool set.
Use a `final` turn. The harness accepts completion only when request-scoped tool
receipts prove any claimed mutation and a later receipt validates the end state.

Summarize this execution result. 

**CRITICAL RULES:**
1. **DIAGNOSE ONLY:** IF THE OUTPUT CONTAINS ERRORS, FAILURES, OR EXIT CODE 1, state this clearly. Analyze the root cause (often at the end of stack traces).
2. **NO ADVICE:** Do NOT offer "Recommended next steps" or "Try this". Do NOT tell the agent what to do next.
3. **NO BROWSING SUGGESTIONS:** NEVER suggest running `ls`, `cd`, `find`, or `dir` to "verify" files. The agent is forbidden from browsing. 
4. **FACTS FIRST:** Preserve specific file paths, line numbers, and error codes exactly.

Context: {ctx}
Output: {safe_out}
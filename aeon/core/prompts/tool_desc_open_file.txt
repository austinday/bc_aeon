Reads one current file into working memory and returns a SHA-256 receipt. Use
the receipt as `expected_sha256` for a later edit. Files already open stay synced;
do not reopen them unless a tool reports that the on-disk version changed.

Parameters:
- file_path (str, required): relative or absolute path to a specific file.

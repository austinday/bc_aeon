--- a/vllm/models/qwen3_8_flash_next/nvidia/mtp.py
+++ b/vllm/models/qwen3_8_flash_next/nvidia/mtp.py
@@ -331,12 +331,13 @@
     def load_weights(self, weights: Iterable[tuple[str, torch.Tensor]]) -> set[str]:
         weights = maybe_fuse_shared_experts(
             weights,
             n_routed_experts=getattr(self.config, "num_experts", 0) or 0,
             n_shared_experts=1,
             ckpt_prefix="mlp.shared_expert",
+            enabled=False,
         )
         loader = AutoWeightsLoader(
             self,
             skip_substrs=["hyper_connection_mixer.block_inject_weight"],
             ignore_unexpected_suffixes=_QWEN38_FLASH_NEXT_IGNORED_MISSING_SUFFIXES.copy(),
         )
--- a/vllm/model_executor/models/qwen3_next.py
+++ b/vllm/model_executor/models/qwen3_next.py
@@ -160,5 +160,12 @@
         _fse_requested = rocm_aiter_ops.is_fusion_moe_shared_experts_enabled()
         _fse_enabled = _fse_requested and _is_shared_expert_fse_compatible(quant_config)
+        # The released MTP checkpoint intentionally keeps its shared expert
+        # in BF16 while its routed experts use packed ModelOpt NVFP4.  Draft
+        # prefixes therefore need an explicit unquantized shared-expert
+        # construction; the generic quant config cannot express two specs
+        # inside one sparse block reliably after the draft layer remap.
+        is_mtp_draft = prefix.startswith("mtp.layers.") or ".mtp.layers." in prefix
+        shared_expert_quant_config = None if is_mtp_draft else quant_config
         if _fse_requested and not _fse_enabled:
             logger.warning(
                 "VLLM_ROCM_USE_AITER_FUSION_SHARED_EXPERTS is enabled but "
@@ -175,7 +182,7 @@
                 hidden_size=config.hidden_size,
                 intermediate_size=config.shared_expert_intermediate_size,
                 hidden_act=config.hidden_act,
-                quant_config=quant_config,
+                quant_config=shared_expert_quant_config,
                 reduce_results=False,
                 expert_gate=self.shared_expert_gate,
                 is_sequence_parallel=self.is_sequence_parallel,

--- a/vllm/models/qwen3_8_flash_next/nvidia/ple_layer.py
+++ b/vllm/models/qwen3_8_flash_next/nvidia/ple_layer.py
@@ -29,7 +29,6 @@
 )
 from vllm.model_executor.layers.quantization.fp8 import Fp8Config
 from vllm.model_executor.layers.quantization.utils.fp8_utils import (
-    create_fp8_scale_parameter,
     create_fp8_weight_parameter,
     is_fp8,
 )
@@ -40,7 +39,6 @@
     VocabParallelEmbedding,
 )
 from vllm.model_executor.models.utils import AutoWeightsLoader
-from vllm.model_executor.parameter import PerTensorScaleParameter
 from vllm.transformers_utils.configs.qwen3_8_flash_next import (
     Qwen3_8FlashNextTextConfig,
 )
@@ -160,18 +158,11 @@
         weight = create_fp8_weight_parameter(
             sum(output_partition_sizes), input_size_per_partition, weight_loader
         )
+        # The checkpoint's single global PLE scale is loaded as a buffer by
+        # Qwen3_8FlashNextNGramEmbedding.load_weights. Registering it as a
+        # parameter here would collide with that buffer during model loading.
         layer.register_parameter("weight", weight)
 
-        weight_scale = create_fp8_scale_parameter(
-            PerTensorScaleParameter,
-            output_partition_sizes,
-            input_size_per_partition,
-            None,
-            weight_loader,
-            scale_dtype=torch.bfloat16,
-        )
-        layer.register_parameter("weight_scale", weight_scale)
-
     def apply(
         self,
         layer: nn.Module,
@@ -187,9 +179,16 @@
 def _get_ple_embedding_quant_method(
     quant_config: QuantizationConfig | None,
     prefix: str,
+    ple_embedding_dtype: str | None = None,
 ) -> QuantizeMethodBase | None:
     """Select global-scale FP8 only for quantized PLE checkpoint shards."""
 
+    # Hybrid ModelOpt checkpoints keep routed experts in NVFP4 while their
+    # PLE table remains FP8. The text config, rather than the model-level
+    # quantization config, is authoritative for this mixed checkpoint.
+    if ple_embedding_dtype == "float8_e4m3fn":
+        return Qwen3_8FlashNextPLEFp8EmbeddingMethod()
+
     if not isinstance(quant_config, Fp8Config):
         return None
     if not quant_config.is_checkpoint_fp8_serialized:
@@ -286,7 +284,9 @@
             padding_size=divisor,
             prefix=f"{prefix}.ngram_embedding",
             quant_method=_get_ple_embedding_quant_method(
-                quant_config, f"{prefix}.ngram_embedding"
+                quant_config,
+                f"{prefix}.ngram_embedding",
+                getattr(config, "ple_embedding_dtype", None),
             ),
         )
         self.register_buffer(
@@ -524,6 +524,17 @@
                 )
                 loaded.add("ngram_embedding.weight")
                 continue
+            # Hybrid ModelOpt metadata can omit this PLE embedding from the
+            # generic quantization map. Retain the exported global scale so
+            # forward() can dequantize the FP8 lookup result.
+            if name.endswith("ngram_embedding.weight_scale"):
+                self.ngram_embedding.register_buffer(
+                    "weight_scale",
+                    loaded_weight.to(device=self.ngram_embedding.weight.device),
+                    persistent=True,
+                )
+                loaded.add("ngram_embedding.weight_scale")
+                continue
             regular_weights.append((name, loaded_weight))
 
         if regular_weights:

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["aeon/tests"]
norecursedirs = ["aeon_output", "build", "dist", ".git"]
